# prgp / docs — Spec

Protocol specification. Files are versioned as `prgp-v<N.M>.md` (pinned) and `prgp-v<N.M>-draft.md` (current state).

The current spec draft lives in the **agent scope** at `scopes/problem-graph.protocol/docs/prgp-v0.2-draft.md` and will graduate here once stable. Until graduation, this directory holds only Architecture Decision Records (ADRs) and supplementary notes.
